{
  "name": "ggseg3d",
  "version": "2.0.0",
  "private": true,
  "description": "JavaScript dependencies for ggseg3d R package",
  "scripts": {
    "postinstall": "npm run build",
    "build": "esbuild node_modules/three/build/three.module.js --bundle --global-name=THREE --format=iife --minify --outfile=inst/htmlwidgets/lib/three/three.min.js"
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
